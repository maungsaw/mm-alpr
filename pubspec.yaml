name: mm_alpr
description: "Myanmar Automatic License Plate Recognition."
version: 0.0.1
homepage:

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  camera:
  google_mlkit_text_recognition:
  intl:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
